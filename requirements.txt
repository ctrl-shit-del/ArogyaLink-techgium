# SYNERA 2.0 — No Docker. Supabase + pgvector + Groq/Ollama.
# pip install -r requirements.txt && python run.py

# Web framework
fastapi
uvicorn[standard]

# Supabase (replaces Postgres + ChromaDB + Redis)
supabase
vecs

# LLM
groq
ollama
langchain
langchain-groq
langchain-community

# RAG / embeddings + reranker
cohere>=5.0.0
sentence-transformers
# cross-encoder  # optional; reranker currently disabled in code

# Data
sqlalchemy[asyncio]
asyncpg
pydantic
pydantic-settings
numpy

# DRL Triage Agent (Section 4.4)
stable-baselines3==2.3.2
gymnasium==0.29.1
torch

# Dev / test
pytest
pytest-asyncio
httpx
python-dotenv

